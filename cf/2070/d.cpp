#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    int a[n - 1];
    vector<vector<int>> levels;
    for (int i = 0; i < n - 1; i++) {
      cin >> a[i];
      if (a[i] == 1) {
      }
    }
  }
}
